#include <stdio.h>

/**
 * main - entry point(START)
 *	print the name of program, then exit.
 * @argc: number/count of arguments
 * @argv: list of arguments
 *
 * Return: 0
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
